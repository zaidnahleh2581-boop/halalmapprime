import SwiftUI

struct CommunityHubScreen: View {
    @EnvironmentObject var lang: LanguageManager

    var body: some View {
        Text(lang.isArabic ? "صفحة المجتمع" : "Community hub")
            .padding()
    }
}

struct FaithToolsScreen: View {
    @EnvironmentObject var lang: LanguageManager

    var body: some View {
        Text(lang.isArabic ? "أدوات الإيمان" : "Faith tools")
            .padding()
    }
}
